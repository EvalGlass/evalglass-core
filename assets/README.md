# `assets/` — plugin brand & demo assets

Reserved for the prism logo, the reproducible demo GIF/cast, and generated diagrams used by the
README and marketplace listing. Populated in **EGP-P2** (the demo is a first-class P2 deliverable).

Assets are a delivery surface — **never vendored** into a host (ADR 0022).
